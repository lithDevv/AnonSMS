# What Is Anonsms? 
```
Anonsms is a SMS spam API writen in PHP that uses the SMTP (simple mail transfer protocol) to message the numbers Gateway which will then send the message thro to the target
```
